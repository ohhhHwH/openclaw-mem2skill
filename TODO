# 创建框架
# 流程设计
# 参考 @ref/插件清单.txt  @ref/构建最小插件.txt   @ref/入口点文档.txt  
创建最小插件，使其能够 获取并修改 用户输入，目前默认输入为 "hello openclaw,"
获取调用技能输入，Agent规划，工具调用等信息，
并保存在日志当中 路径为 ~/workspace/agent/logs/myplugins.log
#FIX 当前日志中只有 {"time":"2026-04-25T13:27:28.065Z","category":"plugin","message":"memory2skill registered"} 在用户输入问题后日志中并没有保存用户输入问题
    #TODO - low: FIX BUG 插件多次被注册 definePluginEntry 有问题
        简短结论：
        插件被多次运行通常是因为 OpenClaw 在不同的注册模式
        （如 discovery / cli-metadata / full）或多次评估入口时
        会调用你的 register，而你的 register(api) 
        在每次调用时不加判断地注册了大量监听器/钩子，导致重复注册与重复运行。
        参见平台文档说明：入口点文档.txt:48。
    #最终回复没有被抓取到 - v1.6成功get到设想所需信息
        添加一个hook使智能体最终回复给用户的信息也被接收
    #在获取到gateway的信息后才开始写日志 - 相当于解决一下多次注册的问题-至少日志中信息少了 日志精简
        仅保留 lifecycle user_input agent_plan tool_call tool_result agent_end llm_output 相关日志

#测试脚本


#TODO 将前期设想合并进来
    # TODO 合并主干部分（query/tool call/ans保存，建图，图匹配，检索）
        # TODO 完成接口函数
    # TODO openclaw中指导Agent做出规划 - 暂定 prompt
    # TODO 以本地日志形式保存图谱数据 (供后续批量导入)
        # TODO storage.ts PATH 路径指定
    # TODO 创建CONFIG文件，包含日志路径，图谱数据路径等

    # TODO 语义标准化拆解 + 图对齐
        # TODO 图中因果关系并非线性的,成图状，to有多个,from有多个 -> 如问题to多个toolcall，最终问题的回答from多个tool call的结果
            # TODO 判断工具调用间的依赖关系 - 找到 Tool call 是由谁规划的
                # TODO agent_end message 中事件链记录数据过多，包括历史规划，减去晚于问题输入的message - 剩下的就是规划
                # TODO 直接用 agent_end 清理过后的 message 建图
                # TODO 找到一个 openclaw需要上一个tool call生成的结果才能进行下一个tool call的问题
        # TODO 语义标准化拆解 - 暂定 prompt
        # TODO 图对齐 - 暂定 prompt
        # TODO 图中 label( query ) -> "label":"今天leetcode每日一题是什么" -> 转成离散标准化的label

    # TODO 用户评价/打分体系->准确性 速度 回答格式 - 暂定用户下一次的query中包含对上一次回答的评价/打分
    # TODO pro
        # TODO 判断工具调用间可以并行的调用
        # TODO 判断tool call的结果之间是否有依赖性
        # TODO 用户评价/打分体系->准确性 速度 回答格式
        # TODO Query 相似度
        # TODO 多用户同时输入，事件链如何建立
            TODO : 日志中如何区分是哪个agent_plan的工具调用？如果有同步的tool call如何区分
    # TODO test
        # test中 保存事件链为相同问题
        # TODO test没有进行 历史事件链检索 的测试，以及历史事件链的向量表和对应原始文本


#TODO 查看赛题说明 https://bytedance.larkoffice.com/wiki/TYewweOPuiHMtBkA1aXcldJonic

# 创建框架
# 流程设计
# 参考 @ref/插件清单.txt  @ref/构建最小插件.txt   @ref/入口点文档.txt  
创建最小插件，使其能够 获取并修改 用户输入，目前默认输入为 "hello openclaw,"
获取调用技能输入，Agent规划，工具调用等信息，
并保存在日志当中 路径为 ~/workspace/agent/logs/myplugins.log
#FIX 当前日志中只有 {"time":"2026-04-25T13:27:28.065Z","category":"plugin","message":"memory2skill registered"} 在用户输入问题后日志中并没有保存用户输入问题
    #TODO - low: FIX BUG 插件多次被注册 definePluginEntry 有问题
        简短结论：
        插件被多次运行通常是因为 OpenClaw 在不同的注册模式
        （如 discovery / cli-metadata / full）或多次评估入口时
        会调用你的 register，而你的 register(api) 
        在每次调用时不加判断地注册了大量监听器/钩子，导致重复注册与重复运行。
        参见平台文档说明：入口点文档.txt:48。
    #最终回复没有被抓取到 - v1.6成功get到设想所需信息
        添加一个hook使智能体最终回复给用户的信息也被接收
    #在获取到gateway的信息后才开始写日志 - 相当于解决一下多次注册的问题-至少日志中信息少了 日志精简
        仅保留 lifecycle user_input agent_plan tool_call tool_result agent_end llm_output 相关日志

#测试脚本

# High Priority

# TODO 图对齐 - 暂定 prompt
    # openclaw 调用LLM
        ...拆解 / 相似度 / 最终回答等
        # bug 结果+ans 分数是 null - {"time":"2026-05-04T06:19:59.016Z","category":"llm_eval","message":"skipped: llmApiUrl or llmApiKey not configured","data":{}}
    # TODO 闲时线程操作 - 图对齐 - 相似事件特征提取/关节步骤提取等
    # TODO 长短期记忆转化
    #fix BUG 没有get到历史事件链的记录 
    #fix BUG 检索后事件链没有加入到agentplan 中
        # v1.16.1 就将其注入了，从agent_end中的对话中了
            # 应该将消息注入到原始消息之后 - 当前返回 ""
    # TODO bug 检索事件链中不要包含 最终回答 给出回答的 正确分数 / 置信度->问题相似性  之前回答的正确性改为->置信度，放在结果处
    
    # TODO reply_dispatch 中 prompt添加有概率不触发？？
        # TODO 会修改原始信息
    # todo before_prompt_build 和 reply_dispatch 重复注入了

    # TODO 复赛文档补图
# Mid Priority

#TODO 将前期设想合并进来
    # TODO 合并主干部分（query/tool call/ans保存，建图，图匹配，检索）
        #完成接口函数
    # TODO openclaw中指导Agent做出规划 - 暂定 prompt - 将检索的信息附加到 query 上
    #以本地日志形式保存图谱数据 (供后续批量导入)
    # TODO 语义标准化拆解 + 图对齐
        # 图中因果关系并非线性的,成图状，to有多个,from有多个 -> 如问题to多个toolcall，最终问题的回答from多个tool call的结果
            # 判断工具调用间的依赖关系 - 找到 Tool call 是由谁规划的
                #agent_end message 中事件链记录数据过多，包括历史规划，减去晚于问题输入的message - 剩下的就是规划
                # 直接用 agent_end 清理过后的 message 建图，因为这里面包含了足够的信息，且放在回复完后操作，节省时间
                #找到一个 openclaw需要上一个tool call生成的结果才能进行下一个tool call的问题 - 找leetcode每日一题最后的那个网址的获取跟前文相关
                    # 前文如何来的？如何建立 因果 关系？-用LLM？ - v1.7 log中缺少agent_end message 信息暂时无法溯源 - 用/new
                        # 该链接从前文中的 tool call 后的 tool_result 中来
                        # 当前认为如果查询的字符串 在之前的 tool_result 中能够找到（字符串匹配），那么认为是depend依赖关系 - 暂定
                # TODO 将 think + prompt 投入 LLM 给出 依赖关系 和 上次 func call 评价！对上次 funcall 打分
        # TODO 图中 label( query ) -> "label":"今天leetcode每日一题是什么" -> 转成离散标准化的label
            # TODO 语义标准化拆解 - 暂定 prompt - 给问题打标签 拆解 - 类似与 kimi
            # TODO Query 相似度
    # TODO 最终回答 + query -> LLM 给出一个分数
        # TODO 事件链检索 - 结果反馈 - 用LLM
        # TODO 结果低于 阈值 其图不做保存
    # TODO message_received 将原始信息 + retrival
        # TODO "category":"agent_plan","message":"reply_dispatch" 时，并没有接受到 retrival 中添加的事件链等信息
    # log 中 保留事件触发 message_received agent_end；日志保留  reply_dispatch llm_input；其他 删去(暂定-成功规划中用不到)
    # test
        # TEST 中有bug 
        # test中 保存事件链为相同问题
        # test没有进行 历史事件链检索 的测试，以及历史事件链的向量表和对应原始文本
    # TODO 当/new等特殊指令时不做记录等操作
# Low Priority
    # TODO 创建CONFIG文件，包含日志路径，图谱数据路径等
    # TODO storage.ts PATH 路径指定
    # TODO 暂定用户下一次的query中包含对上一次回答的评价/打分
    # TODO 用户评价/打分体系->准确性 速度 回答格式
    # TODO 多用户同时输入，事件链如何建立
    # TODO : 日志中如何区分是哪个agent_plan的工具调用？如果有同步的tool call如何区分 - 日志上看不出来，暂时没有并行调用吧
    # TODO : LOG 加入统一配置中去
#TODO 查看赛题说明 https://bytedance.larkoffice.com/wiki/TYewweOPuiHMtBkA1aXcldJonic